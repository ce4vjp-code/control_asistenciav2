<Window x:Class="AsistenciaDesktop_v2.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Ingreso - Control de Asistencia v2" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Icon="pack://application:,,,/Assets/logo.png"
        Background="{DynamicResource AppBackground}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <materialDesign:Card Width="320" Height="400" UniformCornerRadius="8" Padding="20">
            <StackPanel VerticalAlignment="Center">
                <Image Source="pack://application:,,,/Assets/logo.png" Height="80" Margin="0,0,0,20"/>
                
                <TextBlock Text="INICIAR SESIÓN" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                <TextBox x:Name="txtUsername"
                         materialDesign:HintAssist.Hint="Usuario"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Margin="0,0,0,15" FontSize="16" />

                <PasswordBox x:Name="txtPassword"
                             materialDesign:HintAssist.Hint="Contraseña"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             Margin="0,0,0,25" FontSize="16" KeyDown="txtPassword_KeyDown" />

                <Button x:Name="btnLogin" Content="INGRESAR" Click="BtnLogin_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="45" FontSize="16" Margin="0,0,0,10"/>
                
                <TextBlock x:Name="txtError" Foreground="Red" TextAlignment="Center" TextWrapping="Wrap" />
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
